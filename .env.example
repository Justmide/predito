# Example environment variables for local development
# Set a local API URL to run against your own backend
VITE_API_BASE_URL=https://predito-middleware.onrender.com/api/v1
# Optional: separate auth base (defaults to $VITE_API_BASE_URL + /auth if not set)
VITE_AUTH_API_BASE_URL=https://predito-middleware.onrender.com/api/v1/auth
